# Common mechanistic-head artifacts

Reusable Python implementations live in `vlm_eval/mechanistic_heads/`. This
directory holds only study-level schemas, compact manifests, and documentation;
external data and large activation stores are ignored.
